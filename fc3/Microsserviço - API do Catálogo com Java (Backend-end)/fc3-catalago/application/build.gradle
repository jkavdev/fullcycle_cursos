plugins {
    id 'java-conventions'
    id 'java-library'
}

group 'br.com.jkavdev.fullcycle.catalogo.application'

dependencies {
    implementation(project(":domain"))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}